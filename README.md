Safe Smart Account for Modules
==============

Usage
-----
### Install `remixd` with npm:

```bash
npm install -g @remix-project/remixd
```

### Serve Local Filesystem

To run start `remixd` on default configuration:

```bash
npx remixd
```

On Remix WebUI, connect to Local Filesystem